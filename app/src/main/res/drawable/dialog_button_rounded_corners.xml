<!-- rounded_corners.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/_17sdp" /> <!-- adjust the radius as needed -->
    <solid android:color="@color/buttonColor" /> <!-- optional: set a background color -->
</shape>
